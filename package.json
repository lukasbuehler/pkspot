{
  "name": "pkspot",
  "version": "1.0.3",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "serve": "ng serve",
    "dev": "ng serve --watch --configuration=dev",
    "build": "ng build && node copy-proxy-server.js",
    "build:dev": "ng build --configuration=dev && node copy-proxy-server.js",
    "build:prod": "ng build --configuration=production && node copy-proxy-server.js",
    "test": "ng test",
    "lint": "ng lint",
    "e2e": "ng e2e",
    "deploy:rules:firestore": "firebase deploy --only firestore:rules",
    "deploy:rules:storage": "firebase deploy --only storage:rules",
    "deploy:test:rules": "firebase use test && firebase deploy --only firestore:rules",
    "deploy:prod:rules": "firebase use prod && firebase deploy --only firestore:rules",
    "deploy:functions": "cd ./functions && npm run build && firebase deploy --only functions && cd ..",
    "deploy:test:functions": "firebase use test && cd ./functions && npm run build && firebase deploy --only functions && cd ..",
    "deploy:prod:functions": "firebase use prod && cd ./functions && npm run build && firebase deploy --only functions && cd .."
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^20.0.6",
    "@angular/cdk": "^20.0.5",
    "@angular/common": "^20.0.6",
    "@angular/compiler": "^20.0.6",
    "@angular/core": "^20.0.6",
    "@angular/fire": "^20.0.1",
    "@angular/forms": "^20.0.6",
    "@angular/google-maps": "^20.0.5",
    "@angular/localize": "^20.0.6",
    "@angular/material": "^20.0.5",
    "@angular/platform-browser": "^20.0.6",
    "@angular/platform-browser-dynamic": "^20.0.6",
    "@angular/platform-server": "^20.0.6",
    "@angular/router": "^20.0.6",
    "@angular/service-worker": "^20.0.6",
    "@angular/ssr": "^20.0.5",
    "@firebase/auth": "^1.8.2",
    "@firebase/firestore": "^4.7.6",
    "@firebase/storage": "^0.13.5",
    "@videogular/ngx-videogular": "^8.0.0",
    "bootstrap": "^5.3.0",
    "compression": "^1.8.0",
    "express": "^4.18.2",
    "rxjs": "^7.8.1",
    "swiper": "^11.1.7",
    "timers": "^0.1.1",
    "typesense": "^1.7.2",
    "uuid": "^10.0.0",
    "xml2js": "^0.6.0",
    "zone.js": "^0.15.0"
  },
  "devDependencies": {
    "@angular-devkit/core": "^20.0.5",
    "@angular/build": "^20.0.5",
    "@angular/cli": "^20.0.5",
    "@angular/compiler-cli": "^20.0.6",
    "@angular/language-service": "^20.0.6",
    "@types/core-js": "^2.5.5",
    "@types/express": "^4.17.17",
    "@types/node": "^18.18.0",
    "@types/xml2js": "^0.4.9",
    "ng-extract-i18n-merge": "^3.0.0-0",
    "sass": "^1.63.3",
    "schema-dts": "^1.1.5",
    "typescript": "5.8.3"
  }
}
